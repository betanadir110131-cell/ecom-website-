import ContactClient from './ContactClient';

export const metadata = {
  title: 'Contact Us - TechCommerce',
  description: 'Get in touch with TechCommerce support team',
};

export default function ContactPage() {
  return <ContactClient />;
}